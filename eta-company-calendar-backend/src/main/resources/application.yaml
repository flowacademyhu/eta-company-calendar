spring:
  profiles:
    - default
    - dev
  datasource:
    url: jdbc:mysql://localhost:3306/companycalendar
    username: companycalendar
    password: KkN7{/uvw}?CKi).hDY8YSM0}YMC3QT

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create-drop

mailing:
  mailgun:
    apiKey: ${MAILGUN_API_KEY:}
    mailSenderUrl: "https://api.mailgun.net/v3/sandbox2b4aedba15744847a5e9bbf5f3343ad3.mailgun.org"
  mailFrom: "Mailgun Sandbox <postmaster@sandbox2b4aedba15744847a5e9bbf5f3343ad3.mailgun.org>"
  messageTemplate: "Hello %s, értekezleted van %s - %s a %s teremben."

# mailgun: apiKey, mailSenderUrl, mailFrom

---
spring:
  profiles: travis
  datasource:
    url: jdbc:postgresql://localhost:5432/companycalendar
    username: companycalendar
    password: KkN7{/uvw}?CKi).hDY8YSM0}YMC3QT
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        #                dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true

mailing:
  mailgun:
    apiKey: ${MAILGUN_API_KEY:}
    mailSenderUrl: "https://api.mailgun.net/v3/sandbox2b4aedba15744847a5e9bbf5f3343ad3.mailgun.org"
  mailFrom: "Mailgun Sandbox <postmaster@sandbox2b4aedba15744847a5e9bbf5f3343ad3.mailgun.org>"
  messageTemplate: "Hello %s, értekezleted van %s - %s a %s teremben."